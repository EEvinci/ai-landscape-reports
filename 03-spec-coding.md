# Spec Coding：AI 时代的"契约式"开发新范式

## 【开篇场景】

2025 年 GitClear 对 2.11 亿行代码的研究显示：AI 编程工具普及后，代码复制粘贴率上升了 **48%**，而真正的重构活动下降了 **60%**。这组数字揭示了一个隐藏危机——AI 提高了代码产量，却降低了代码质量。行业的回应是：重新把**规格说明书（Spec）**放在开发流程的中心。这个新范式被命名为 **Spec Coding**（规格驱动开发）。

## 【核心是什么】

Spec Coding 是 AI 时代的软件开发方法论，核心理念是：**规格文档是唯一的事实来源（Single Source of Truth），所有代码必须从规格严格派生**。

程序员的核心工作从"手写代码"迁移到"编写、审查和维护规格说明书"。AI Agent 的角色被明确定位：执行规格约束内的任务，而不是自由发挥。这解决了 AI 编程最大的两个痛点——**幻觉（hallucination）**和**不可追溯性（untraceability）**。

## 【关键原理】

把 Spec Coding 理解为"工程契约化"：人类给 AI 签署一份精确的合同，规定"做什么、怎么做、在什么边界内做"。合同之外的行为，一律视为违约。

**OpenSpec 框架的实践路径**

Fission-AI 推出的 OpenSpec（MIT 开源）是目前最完整的 Spec Coding 实现框架，核心工作流只有三条命令：

1. **`/opsx:propose`**：输入需求，AI 自动生成四类制品——`proposal.md`（为什么做）、`specs/`（验收标准，WHEN/THEN 格式）、`design.md`（技术方案）、`tasks.md`（执行清单）
2. **`/opsx:apply`**：AI 按任务清单逐条执行，每次只读取当前任务+相关规格上下文，极大压缩 AI 的"自由发挥空间"，幻觉概率随之降低
3. **`/opsx:archive`**：完成后归档，形成项目的知识持久化——即使更换 AI 模型，新模型也能通过阅读规格快速理解项目全貌

**与"Vibe Coding"的根本区别**

Thoughtworks 在 2025 年 12 月的分析中精确定义了两者的边界：Vibe Coding 是"直觉驱动、即时 Prompt、即兴发挥"，适合 0→1 探索阶段；Spec Coding 是"结构化文档、契约约束、工业流水线"，适合 1→10 迭代阶段。GitClear 数据表明，仅靠 Vibe Coding 的团队正在积累大量技术债务——代码可维护性低、新人接手困难、AI 生成代码难以审计。

## 【真实案例】

**案例 1：Thoughtworks 的 SDD 实践**
Thoughtworks 在 2025 年底的 Tech Radar 中将 Spec-Driven Development 列为" Adopt"（推荐采用）级别。他们在多个企业项目中实践了 SDD 流程：需求分析→规格编写→AI 生成代码→人工审查→自动化测试验证。关键是引入了 BDD 风格的 Given/When/Then 场景格式，让规格文档既能被人类理解，也能被 AI 准确执行。

**案例 2：GitHub Spec Kit**
GitHub 在 2025 年推出了 Spec Kit，将规格管理直接集成到 Pull Request 流程中。开发者可以创建 `SPEC.md`，AI Agent 在生成代码时必须引用对应的 SPEC 条款，任何偏离规格的修改都会被自动标记。这个机制使得代码审查从"看代码对不对"变成了"看代码是否符合规格"，大幅降低了审查成本。

**案例 3：GitClear 数据的警示**
GitClear 的 2.11 亿行代码分析揭示了一个核心问题：AI 辅助编程导致代码相似度大幅上升，而架构级重构（有意义的大范围代码改造）却减少了。这意味着团队在快速交付功能的同时，正在以牺牲系统长期健康为代价。Spec Coding 的支持者认为，这是 Spec 缺失的直接后果——没有规格约束，AI 生成的就是"能跑"的代码，而不是"正确的"代码。

## 【趋势与机会】

**1–3 年判断：Spec Coding 将在 2026 年成为企业级 AI 编程的事实标准。**

个人开发者和早期创业公司可能继续用 Vibe Coding 快速原型，但任何需要长期维护的企业系统，最终都必须引入规格管理。

**机会点：**
- **规格验证工具**：自动检查 AI 生成的代码是否与 SPEC.md 对齐，类似代码质量工具但面向规格层
- **规格搜索引擎**：在大规模代码库中搜索"哪个 SPEC 条款导致了某个 bug"，建立规格-代码-缺陷的完整溯源链
- **合规审计工具**：金融、医疗等受监管行业需要"规格-代码"的完整映射链来满足监管要求，这本身就是 Spec Coding 的核心价值

**需要警惕的风险：** 过度规格化会拖慢开发速度。好的 Spec Coding 不是给每个函数写规格，而是给核心业务逻辑和跨模块接口建立精确规格，细节实现保持灵活。这是工程判断力的体现，而不是工具能替你做的。
