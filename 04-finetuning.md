# Fine-tuning（微调）

## 【开篇场景】

你的医疗 AI 产品在通用测试里表现优秀，但遇到"请根据这份 CT 报告给出诊断建议"时，总是输出模棱两可的套话——因为它从来没有见过真实的 CT 报告，也没有被训练过医疗对话的语气和边界。后来团队用 5000 条标注好的医疗问答数据对基础模型做了微调，同样的问题，现在能给出带具体科室建议、引用最新临床指南的答案，还能主动提示"建议线下就诊"。这就是 Fine-tuning——在已经训练好的大模型基础上，用特定领域数据做二次训练，让模型"长出"专有能力。

## 【核心是什么】

大模型的"通才"与"专才"之分，就是预训练和微调的核心差异。预训练让模型学会语言理解和生成的一般能力（比如语法、世界知识、逻辑推理）；微调则是在这个基础上，用特定任务的标注数据教会模型"在这个场景下怎么答才对"。

微调解决的核心问题不是"让模型知道更多"，而是**"让模型在特定场景下行为更可控、更精准、更符合预期"**。它需要的数据量比从头训练少得多，成本通常是预训练的千分之一甚至万分之一，但效果提升往往非常显著。

主流微调方法包括：SFT（监督微调，直接用标注数据教模型正确答案）、LoRA（低秩适配，用少量参数模拟全量微调效果，2025 年已成为主流）、RLHF（结合人类反馈，后文单独讨论）。

## 【关键原理】

想象一个已经学过中文语言的人，现在要让他成为专业律师。你不需要再教他认字、说话——那些能力已经有了；你要做的是让他熟悉法律术语、理解案件分析逻辑、知道什么该说什么不该说。Fine-tuning 就是这个"专业化训练"的过程。

LoRA 是 2025 年最主流的微调方法，原因是它极其高效：不再调整模型的所有参数（那需要 GPU 显存大到不可能），而是只训练两组小矩阵（低秩矩阵），然后把它们加到原模型的权重上。你可以理解为：在不变动主建筑结构的情况下，通过外挂装置让模型"长出"新能力。Meta 开源的 Llama 系列模型，加上 LoRA 微调，是 2025 年中小企业构建私有模型的主流组合。

微调的关键陷阱是"灾难性遗忘"——微调后的模型在新任务上表现好了，但在通用能力上退步了。解决方案是 DPO（Direct Preference Optimization）等方法，它们在优化新能力的同时保留原有能力，效果优于传统的 SFT。

## 【真实案例】

GPT-4o 在 2025 年初开放了"自定义微调"API，企业可以用自己的业务数据微调专属版本。某国际快消品牌用 10 万条客服对话记录微调后，客服 Bot 的满意度评分从 3.2/5 提升到 4.6/5，单次对话成本下降了 40%——因为微调后的模型不需要那么长的 Prompt 就能给出精准答案，推理成本自然下降。

Mistral AI 在 2024 年底发布的 Mixtral 8x22B 模型，被欧洲多个金融机构微调后用于风险评估场景。法国巴黎银行用微调后的模型做贷款审批初筛，误拒率（好客户被错误拒绝）下降了 18%，同时没有增加坏账率——这是一个直接影响利润的 AI 应用。

在国内，智谱 GLM-4、百川大模型都在 2025 年推出了"一键微调"平台，降低了微调的技术门槛。某在线教育公司用微调后的模型做作文批改，能识别出"初一学生常见的比喻不当问题"并给出符合该年龄段认知水平的修改建议，学生评分比人工批改更高。

## 【趋势与机会】

**未来 1-3 年判断：**

1. **微调从"专家专属"变成"产品标配"**——随着微调工具越来越傻瓜化（阿里云 PAI、百度智能云都已支持可视化微调），未来每个垂直 SaaS 产品都可能自带一个微调模块，根据用户行为数据持续优化模型表现。

2. **微调 + RAG 的组合成为主流架构**——微调让模型"懂行规"，RAG 让模型"知道最新信息"。两者结合：RAG 负责知识补充，微调负责风格和判断标准，是 2025-2026 年企业 AI 落地的主流技术选型。

3. **微调数据的质量成为核心竞争力**——模型开源了，微调方法公开了，但高质量的领域标注数据是稀缺的。拥有金融、医疗、法律等垂直领域高质量微调数据集的公司，其数据资产价值会被重估。
